# This file contains the different endpoints your bot can use.

# Server where the models are pulled from.
# https://rasa.com/docs/rasa/user-guide/running-the-server/#fetching-models-from-a-server/

#models:
#  url: http://my-server.com/models/default_core@latest
#  wait_time_between_pulls:  10   # [optional](default: 100)

# Server which runs your custom actions.
# https://rasa.com/docs/rasa/core/actions/#custom-actions/

action_endpoint:
 url: "http://localhost:5055/webhook"

# Tracker store which is used to store the conversations.
# By default the conversations are stored in memory.
# https://rasa.com/docs/rasa/api/tracker-stores/

#tracker_store:
#    type: redis
#    url: <host of the redis instance, e.g. localhost>
#    port: <port of your redis instance, usually 6379>
#    db: <number of your database within redis, e.g. 0>
#    password: <password used for authentication>
#    use_ssl: <whether or not the communication is encrypted, default false>

# tracker_store:
#    type: mongod
#    url: mongodb+srv://cluster0.fkolv.mongodb.net
#    db: chatbot-log
#    username: zkm-user
#    password: s2CxPPeeLPEkkV5o
#tracker_store:
        #type: SQL
      #dialect: "postgresql"  # the dialect used to interact with the db
      #url: "postgrespdi.cuuomhv1yn0u.eu-central-1.rds.amazonaws.com"
      #db: "zkm"  # path to your db
      #username: "postgres"  # username used for authentication
      #      password: "Qnf66hZs5-ZL" # password used for authentication
     #  query: # optional dictionary to be added as a query string to the connection URL
      #  driver: my-driver

 #tracker_store:
 #     type: SQL
 #     dialect: "mysql"  # the dialect used to interact with the db
 #     url: "chatbot8.zkm.de"
 #     db: "chatbot"  # path to your db
 #     username: "integr8"  # username used for authentication
 #     password: "VPrYkWrsh9QekJZ2" # password used for authentication
     #  query: # optional dictionary to be added as a query string to the connection URL
      #  driver: my-driver

# Event broker which all conversation events should be streamed to.
# https://rasa.com/docs/rasa/api/event-brokers/

#event_broker:
#  url: localhost
#  username: username
#  password: password
#  queue: queue
#store_type: tracker_store.CustomSQLTrackerStore

 #tracker_store:
 #     type: SQL
 #     dialect: "mysql"  # the dialect used to interact with the db
 #     url: "db"
 #     db: "chatbot"  # path to your db
 #     username: "integr8"  # username used for authentication
 #     password: "VPrYkWrsh9QekJZ2" # password used for authentication
     #  query: # optional dictionary to be added as a query string to the connection URL
      #  driver: my-driver
